@model AMassage.Models.Booking

@{
    Layout = "_Layout";
    ViewBag.Title = "Удаление временного слота";
}

<h1>Удаление временного слота</h1>
<p>Вы уверены, что хотите удалить временный слот?</p>

<form method="post" asp-controller="Booking" asp-action="Delete" asp-route-id="@Model.Id">
    <button type="submit" class="btn btn-danger">Да, удалить</button>
    <a href="/Booking" class="btn btn-secondary">Отмена</a>
</form>